name: doorboxapp
description: "DoorBox - Smart IoT Parcel Locker"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.0.1
  google_fonts: ^6.2.1
  lucide_icons: any
  intl: ^0.19.0
  socket_io_client: ^3.1.4
  http: ^1.6.0
  dio: ^5.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
