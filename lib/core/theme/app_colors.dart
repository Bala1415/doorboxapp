import 'package:flutter/material.dart';

class AppColors {
  // Primary & Accent
  static const Color primary = Color(0xFF0F3057); // Deep Blue
  static const Color accent = Color(0xFF008891); // Teal

  // Status Colors
  static const Color success = Color(0xFF4CAF50); // Green
  static const Color warning = Color(0xFFFF9800); // Orange
  static const Color error = Color(0xFFF44336); // Red

  // Backgrounds & Surfaces
  static const Color background = Color(0xFFF4F6F9); // Soft light neutral
  static const Color surface = Colors.white; // Card surface
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);

  // Shadows
  static const Color shadowColor = Color(0x14000000); // 8% opacity black
}
